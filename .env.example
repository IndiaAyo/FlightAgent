# FlightAgent .env example
# Copy this file to .env and fill in real values before running the app.

############################################
# Azure OpenAI (recommended for Azure users)
# Set both endpoint and key for Azure OpenAI usage.
# Example endpoint: https://my-openai-resource.openai.azure.com
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_CHAT_DEPLOYMENT_NAME=

############################################
# OR: OpenAI API (if you want to use OpenAI directly)
OPENAI_API_KEY=
OPENAI_CHAT_MODEL_ID=

############################################
# SerpAPI (required by the FlightSearch plugin)
SERP_API_KEY=

############################################
# Foundry / Project endpoint (for reference only)
# NOTE: If this points to an Azure Foundry project endpoint (contains '/api/projects/')
# it is NOT compatible with the Azure OpenAI chat client used in this example.
# If you need to call Foundry, implement a Foundry-specific flow.
PROJECT_ENDPOINT=

############################################
# Helpful local dev setting: use certifi bundle for SSL on macOS venvs
# (set this in your shell or leave empty; app.py attempts to auto-use certifi)
# SSL_CERT_FILE=$(python -m certifi)
